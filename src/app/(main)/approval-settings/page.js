import React from "react";
import ApprovalSettings from "@/components/app-modules/ApprovalSettings";

export const metadata = {
  title: "Approval Settings - API HRM",
};

const Page = () => {
  return <ApprovalSettings />;
};

export default Page;
